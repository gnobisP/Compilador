app serie 
  int num, cont, serie; 
 {
  print("Informe a quantidade de termos: ");  
  scan(num); 
 
  if (num< 0) {
    print("Numero invalido"); }
  else { 
          //calcula serie 
 
          while(cont<=num){ 
            serie = serie + cont; 
            cont = cont + 1; 
          } 
 
          print("O resultado e; "; 
          print(fat); 
       } 
} 